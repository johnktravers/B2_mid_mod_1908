<h1><%= @instructor.name %></h1>

<h3>Students:</h3>

<ul>
  <% @instructor.students.each do |student| %>
    <li><%= student.name %></li>
  <% end %>
</ul>

<h4>Average Age of Students: <%= @instructor.students.average_age.round(1) %></h4>
